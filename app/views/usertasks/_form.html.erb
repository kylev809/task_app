<%= simple_form_for(@usertask) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.input :due %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
